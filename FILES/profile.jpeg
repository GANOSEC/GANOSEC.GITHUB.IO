<?php
session_start();

// Regenerate the session ID to prevent session fixation
session_regenerate_id(true);

// Check if the user is not logged in, redirect them to the login page
if (!isset($_SESSION['username'])) {
    header('Location: login.php');
    exit();
}



?>

<!DOCTYPE html>
<html>
<head>
    <title>Settings</title>
    <style>
        body {
            background-color: #222;
            font-family: Arial, sans-serif;
            margin: 0;
        }

        .sidebar {
            height: 100%;
            width: 250px;
            position: fixed;
            top: 0;
            left: 0;
            background-color: #333;
            padding-top: 20px;
        }

        .sidebar a {
            padding: 10px 25px;
            text-decoration: none;
            font-size: 18px;
            color: #fff;
            display: block;
        }

        .content {
            margin-left: 250px;
            padding: 20px;
        }

        h1 {
            color: #fff;
        }

        a {
            color: #0073e6;
            text-decoration: none;
        }

        a:hover {
            text-decoration: underline;
        }
               h1 {
            font-size: 36px;
            text-align: center;
            text-transform: uppercase;
        }

        form {
            text-align: center;
        }

        label {
            font-size: 20px;
            display: block;
            text-transform: uppercase;
            color: #fff;
        }

        input[type="text"] {
            font-size: 20px;
            padding: 5px;
            border: 2px solid #00ffcc;
            border-radius: 5px;
            background-color: #08151a;
            color: #00ffcc;
        }

        input[type="text"]:focus {
            outline: none;
            border: 2px solid #00b3b3;
        }

        button {
            font-size: 20px;
            background-color: #00b3b3;
            color: #08151a;
            border: none;
            border-radius: 5px;
            padding: 10px 20px;
            cursor: pointer;
        }

        button:hover {
            background-color: #00ffcc;
            color: #08151a;
        }
				.a-btn {
			
			display: block;
                    width: 120px;
                    text-align: center;
                    background-color: #17a2b8; /* Dark Cyan button background */
                    color: #fff; /* White text color */
                    text-decoration: none;
                    padding: 10px;
                    border-radius: 5px;
                    margin: 20px auto;
                    transition: background-color 0.3s;
		}
		
		/* Style the button */
input[type="submit"] {
  background-color: cyan; /* Background color */
  color: black; /* Text color */
  padding: 10px 20px; /* Padding around the text */
  border: none; /* Remove the default button border */
  border-radius: 5px; /* Rounded corners */
  font-size: 16px; /* Text size */
  cursor: pointer; /* Add a pointer cursor on hover */
  transition: background-color 0.3s, color 0.3s; /* Smooth color transition on hover */
}

/* Style the button on hover */
input[type="submit"]:hover {
  background-color: black; /* Background color on hover */
  color: cyan; /* Text color on hover */
}

/* Optional: Add some extra styling to make it even cooler */
input[type="submit"]:active {
  transform: scale(0.95); /* Slight scaling when clicked */
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.2); /* Drop shadow on click */
}

    </style>
</head>
<body>
    <?php
    $dataFile = 'data.json';

    // Check if the form has been submitted
    if (isset($_POST['number'])) {
        $newNumber = $_POST['number'];

        // Validate the input as a 10-digit number
        if (preg_match('/^\d{10}$/', $newNumber)) {
            // Write the updated number to the data file
            file_put_contents($dataFile, $newNumber);

            $message = 'Number has been updated!';
        } else {
            $message = 'Invalid number. Please enter a 10-digit number.';
        }
    }

    // Read the current number from the data file
    $currentNumber = file_get_contents($dataFile);
    ?>
    <div class="sidebar">
        <a class="a-btn" href="dashboard.php">Dashboard</a>
        <a class="a-btn" href="profile.php">Log Entries</a>
		<a class="a-btn" href="viewsms.php">Messages</a>
        <a class="a-btn" href="settings.php">Settings</a>
        <a class="a-btn" href="ResetPass.php">Change Password</a>
        <a class="a-btn" href="logout.php">Logout</a>
    </div>

    <div class="content">
        <h1>Log Entries</h1>
<?php
// Retrieve the data from the JSON file
$jsonData = file_get_contents('info.json');
$responses = json_decode($jsonData, true) ?: array();

// Pagination
$page = isset($_GET['page']) ? intval($_GET['page']) : 1;
$itemsPerPage = 10;
$totalResponses = count($responses);
$totalPages = ceil($totalResponses / $itemsPerPage);
$offset = ($page - 1) * $itemsPerPage;

// Display the table with pagination


// Retrieve the data from the JSON file
$jsonData = file_get_contents('info.json');
$responses = json_decode($jsonData, true) ?: array();

// Check if a search query is provided
$searchResults = array();
$searchTerm = isset($_GET['search']) ? trim($_GET['search']) : '';

if (!empty($searchTerm)) {
    foreach ($responses as $i => $response) {
        if (stripos($response['fname'], $searchTerm) !== false) {
            $searchResults[] = $response;
        }
    }
}
?>
<!DOCTYPE html>
<html>
<head>
    <title>LoGs</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #0c343b; /* Dark Cyan background */
            color: #fff; /* White text color */
            margin: 0;
            padding: 0;
        }

        h1 {
            text-align: center;
        }

        table {
            width: 80%;
            margin: 20px auto;
            border-collapse: collapse;
        }

        table, th, td {
            border: 1px solid #17a2b8; /* Dark Cyan border */
        }

        th, td {
            padding: 10px;
            text-align: left;
        }

        th {
            background-color: #17a2b8; /* Dark Cyan header background */
            color: #fff;
        }

        td {
            background-color: #154e56; /* Dark Cyan cell background */
        }

        a {
            color: #17a2b8; /* Dark Cyan link color */
            text-decoration: none;
            transition: color 0.3s;
        }

        a:hover {
            color: #0c343b; /* Dark Cyan background on hover */
            background-color: #17a2b8; /* Dark Cyan link color on hover */
        }

        .pagination {
            text-align: center;
            margin-top: 20px;
        }

        .pagination a {
            display: inline-block;
            padding: 5px 10px;
            margin: 0 5px;
            border: 1px solid #17a2b8;
            border-radius: 5px;
        }

        .pagination a:hover {
            background-color: #17a2b8; /* Dark Cyan background on hover */
            color: #fff; /* White text color on hover */
        }
    </style>
</head>
<body>
    <center>
	<form action="search.php" method="get">
        <input type="text" name="search" placeholder="Search by Full Name" value="<?= $searchTerm ?>">
        <input type="submit" value="Search">
    </form>
	</center>

    <table>
        <tr>
            <th>No</th>
            <th>ID</th>
            <th>Full Name</th>
            <th>Expand</th>
        </tr>
        <?php
        for ($i = $offset; $i < min($offset + $itemsPerPage, $totalResponses); $i++) {
            $response = $responses[$i];
            ?>
            <tr>
                <td><?php echo $i + 1; ?></td>
                <td><?php echo $response['ID']; ?></td>
                <td><?php echo $response['fname']; ?></td>
                <td><a href="details.php?id=<?php echo $response['ID']; ?>">View Details</a></td>
            </tr>
            <?php
        }
        ?>
    </table>

    <div class="pagination">
        <?php
        for ($pageNum = 1; $pageNum <= $totalPages; $pageNum++) {
            $pageClass = ($pageNum === $page) ? 'active' : '';
            echo '<a class="' . $pageClass . '" href="profile.php?page=' . $pageNum . '">' . $pageNum . '</a>';
        }
        ?>
    </div>
</body>
</html>

    </div>
      
</body>
</html>
